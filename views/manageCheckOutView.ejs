<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* Reset and general styling */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        h1 {
            text-align: center;
            margin-top: 30px;
            font-size: 2.5rem;
            color: #333;
        }

        .container {
            width: 100%;
            max-width: 900px;
            margin: 30px auto;
            background-color: #ffffff;
            padding: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            flex-grow: 1;
        }

        .reservation {
            display: flex;
            flex-wrap: wrap;
            gap: 20px;
            margin-bottom: 30px;
        }

        .reservation div {
            background-color: #ffffff;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: calc(33% - 20px);
            text-align: center;
            box-sizing: border-box;
        }

        .reservation img {
            width: 100%;
            height: auto;
            border-radius: 8px;
        }

        p {
            font-size: 1rem;
            margin: 10px 0;
            color: #555;
            text-align: left;
        }

        /* Remove underline from anchor tags */
        a {
            text-decoration: none;
            color: inherit;
        }

        button {
            background-color: #007BFF;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s;
            width: 100%;
            margin-top: 10px;
        }

        button:hover {
            background-color: #0056b3;
        }

        footer {
            background-color: #1d1d1d;
            color: white;
            padding: 40px 0;
            text-align: left;
        }

        .footer-container {
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            gap: 40px;
            margin-bottom: 30px;
            padding-left: 70px;
            padding-right: 70px;
        }

        .footer-container div {
            flex: 1;
            min-width: 200px;
        }

        .footer-container div h4 {
            margin-bottom: 10px;
            font-size: 1.2rem;
        }

        .footer-container div p {
            font-size: 0.9rem;
            color: #d8d8d8;
            margin: 5px 0;
        }

        .social-links a {
            margin: 0 10px;
            color: #ffffff;
            text-decoration: none;
        }

        .social-links a:hover {
            color: #007BFF;
        }

        .footer-bottom {
            font-size: 0.8rem;
            color: #ffffff;
            margin-top: 20px;
            padding-left: 70px;
        }

        /* Nav styling */
        nav {
            background-color: #ffffff; /* Ganti dengan latar belakang putih */
            padding: 15px 40px;
            color: white;
            margin-bottom: 0;
            border-radius: 0;
            border-bottom: #d8d8d8 solid 0.3px;
        }

        nav ul {
            list-style: none;
            padding: 0;
            margin: 0;
            display: flex;
            justify-content: flex-start;
            align-items: center;
        }

        nav ul li {
            font-size: 16px;
        }

        nav ul li a {
            color: rgb(0, 0, 0);
            text-decoration: none;
            padding: 8px 16px;
            border-radius: 5px;
        }

        nav ul li a:hover {
            background-color: #007BFF;
        }

        .footer-bottom > p {
          color: white;
          font-size: 0.9rem;
        }
    </style>
</head>
<body>

  <nav>
    <ul>
      <li><a href="/homeReceptionist">Home</a></li>
      <li><a href="">About</a></li>
      <li><a href="/manageCheckInView">Manage Check-In</a></li>
      <li><a href="/manageCheckOutView">Manage Check-Out</a></li>
      <li><a href="/logout">Logout</a></li>
    </ul>
  </nav>

    <div class="container">
        <% if (reservationData.length > 0) { %>
          <div class="reservation">
            <% reservationData.forEach(reservation => { %>
              <div>
                <a href="/detailReservation/<%= reservation.Room.id %>">
                  <img src="<%= reservation.Room.roomImage %>" alt="Room Image">
                  <p>Name: <%= reservation.User ? reservation.User.name : "Guest" %></p>
                  <p><%= reservation.Room.roomName %></p>
                  <p><%= reservation.checkInDate %>$</p>
                  <p><%= reservation.status %></p>
                </a>
                <form action="/validateCheckOut/<%= reservation.id %>" method="post">
                    <input type="hidden" name="roomId" id="hiddenRoomId" value=<%= reservation.Room.id %>>
                    <button type="submit">Validate</button>
                </form>
              </div>
            <% }); %>
          </div>
        <% } else { %>
          <p>No room available currently</p>
        <% } %>
    </div>

    <footer>
        <div class="footer-container">
            <div>
                <h4>Company</h4>
                <p>About Us</p>
                <p>Careers</p>
                <p>Privacy Policy</p>
                <p>Terms of Service</p>
            </div>
            <div>
                <h4>Services</h4>
                <p>Customer Support</p>
                <p>FAQ</p>
                <p>Room Booking</p>
                <p>Contact Us</p>
            </div>
            <div>
                <h4>Follow Us</h4>
                <div class="social-links">
                    <a href="#">Facebook</a>
                    <a href="#">Twitter</a>
                    <a href="#">Instagram</a>
                </div>
            </div>
            <div>
                <h4>Contact</h4>
                <p>Email: support@redwoodhotel.com</p>
                <p>Phone: +1 234 567 890</p>
                <p>Address: 123 Redwood St, City, Country</p>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2025 Redwood Hotel. All Rights Reserved.</p>
        </div>
    </footer>

</body>
</html>
